import React from "react";
import { Button } from "../ui/button";

const CreateNewProject = () => {
  return (
    <Button variant="secondary" className="h-24 hover:bg-neutral-200">
      Create new project
    </Button>
  );
};

export default CreateNewProject;
